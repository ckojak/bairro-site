# Environment Configuration

# Session Secret (REQUIRED in production - generate a strong random string)
# Example: Use `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` to generate
SESSION_SECRET=bairro-secret-key-2024-change-in-production

# Server Port
PORT=3000

# IMPORTANT SECURITY NOTES:
# 1. Always use HTTPS in production to encrypt sensitive data transmission
# 2. Change the SESSION_SECRET to a strong random value
# 3. Consider using a proper database (MongoDB, PostgreSQL, etc.) instead of JSON files
# 4. Implement rate limiting for authentication endpoints
# 5. Add CORS configuration if frontend is hosted separately
